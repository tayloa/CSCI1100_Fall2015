''' Write a program that reads "scores.txt"
each line contians a score and then print scores in decreasing order

Algorithim: 
read file into a list, 
sort the list(reverse), 
then print the list 
'''
#if __name__ == "__main__":
    
    #nums = []
    
    #f = open("scores.txt")   #no actual scores.txt file, just for example
    #for line in f:
        #num = int(line.strip())
        #nums.append(num)
        
    #nums.sort(reverse=True)
    
    #for i in range(len(nums)):
        #print "%d: %d" %(i, nums[i])
        
